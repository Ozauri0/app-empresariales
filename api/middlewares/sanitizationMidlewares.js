// const { body } = require('express-validator');

// // Sanitización para registrar un usuario
// exports.sanitizeRegister = [
//   body('username').trim().escape(),
//   body('email').normalizeEmail(),
//   body('name').trim().escape(),
//   body('password').trim()
// ];

